# Todo-List

A simple todo-list with a minimal look.

## Setup

```
git clone https://github.com/emps725/Todo-List.git
cd Todo-List
npm install
```

## Styles

Tailwind CLI is used to setup the styles.

```
npx @tailwindcss/cli -i ./src/input.css -o ./src/output.css --watch
```
